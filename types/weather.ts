export interface City {
  name: string;
  country: string;
}

export interface Weather {
  date: string;
  temp: string;
  description?: string;
  main?: string;
  pressure?: string;
  humidity?: string;
}
